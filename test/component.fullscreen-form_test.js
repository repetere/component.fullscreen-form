/*
 * component.fullscreen-form
 * https://github.com/typesettin/component.fullscreen-form
 *
 * Copyright (c) 2014 Yaw Joseph Etse. All rights reserved.
 */

'use strict';

var should = require('chai').should();
// var fullscreen_form = require('../lib/component.fullscreen-form');

describe('component.fullscreen-form', function () {
	describe('#indexOf()', function () {
		it('should return -1 when the value is not present', function () {
			should.equal(-1, [1, 2, 3].indexOf(5));
			should.equal(-1, [1, 2, 3].indexOf(0));
		});
	});
});
